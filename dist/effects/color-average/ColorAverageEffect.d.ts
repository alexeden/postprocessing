import { BlendFunction } from '../../blending';
import { Effect } from '../../core';
/**
 * A color average effect.
 */
export declare class ColorAverageEffect extends Effect {
    constructor(blendFunction?: BlendFunction);
}
